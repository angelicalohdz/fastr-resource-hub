# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
dist/
build/

# Virtual environments
.venv/
venv/
env/
ENV/

# Node.js
node_modules/
package-lock.json

# IDE
.idea/
*.swp
*.swo
*~

# VS Code - we DO commit .vscode/ for team Marp config
# (removed from ignore list so settings are shared)

# Outputs - now tracked so they sync between Codespace and local
# (remove these lines if repo gets too large)
!outputs/

# Temp files
*_temp.md
*.tmp
.cache/

# Translation cache (regenerated on demand)
.translation_cache/

# Environment variables (contains API keys)
.env

# Workshop-specific (optional - team decides)
# Uncomment to ignore workshop images (if too large)
# workshops/*/*.png
# workshops/*/*.jpg
# workshops/*/*.svg

# System files
.DS_Store
Thumbs.db
site/
